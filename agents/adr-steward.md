# Agent: adr-steward — Deprecated alias

**Deprecated.** Forwards to **dec-steward**.

When `/adr` is invoked, use the **dec-steward** agent and follow `dec-steward.md` exactly. Output DEC digests only — never ADR files.
