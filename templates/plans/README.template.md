# Plan Files (<project_name>)

All plan files for this project live in `.cursor/plans/`.

- **ACTIVE.plan.md** — pointer to current / last completed plan
- **archive/** — completed plans (read-only history)

Do not save project plans to `~/.cursor/plans/`.

See KSS portable rule: `plans-location.mdc` in `~/.cursor/rules/`.
