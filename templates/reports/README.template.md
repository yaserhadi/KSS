# Project Reports

Canonical location for temporal evidence: `.cursor/reports/`

Reports are **evidentiary** — historical context. They do not override DEC or MANIFEST.

See KSS portable rule: `reports-location-global.md` in `~/.cursor/rules/`.
